import { generateResolvers } from '../schema'

describe('schema', () => {
  describe('smoke tests', () => {
    describe('schema', () => {
      test('should exist a schema', () => {
        console.log(generateResolvers)
      })
    })
  })
})
